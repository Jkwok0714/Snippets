#include <iostream.h>

int main() {
    int counter = 0;
    while (1) {
          counter++;
          cout << ".";
          if (counter >= 10) {
                      break;
                      }
                      }
                      cout << "\nCounter: " << counter << "\n";
                      return 0;
                      }
